import {CommentForm, Comments} from "../components";

const CommentPage = () => {
    return (
        <div>
            <CommentForm/>
            <Comments/>
        </div>
    );
};

export {CommentPage};
